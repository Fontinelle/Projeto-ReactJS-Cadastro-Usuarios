import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import AppJsx from './main/App';

const app = <AppJsx/>
const root = document.getElementById('root')
ReactDOM.render(app, root);
